{
  "name": "expense-tracker",
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend/expense-tracker && npm install",
    "build": "cd frontend/expense-tracker && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}